networks:
  internet:
    internal: false
  private:
    internal: true
  traefik:
    name: traefik-network
    external: true

services:
  db:
    image: "redis:alpine"
    networks:
      - private
    restart: unless-stopped
    volumes:
      - "./data:/data"
  website:
    build: "."
    depends_on:
      - db
    environment:
      - STORAGE_TYPE=redis
      - STORAGE_HOST=db
      - STORAGE_PORT=6379
      - STORAGE_DB=0
    networks:
      - private
      - internet
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.homepage.rule=Host(`bin.hzmi.xyz`)"
      - "traefik.http.routers.homepage.entrypoints=web, websecure"
      - "traefik.http.routers.homepage.tls=true"
      - "traefik.http.routers.homepage.tls.certresolver=letsencrypt"
      - "traefik.http.middlewares.cloudflare-true-ip.plugin.cloudflarewarp.disableDefault=false"
      - "traefik.http.routers.homepage.middlewares=cloudflare-true-ip"
    restart: unless-stopped

version: "3.0"
